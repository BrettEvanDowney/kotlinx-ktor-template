
/**
 * A loadable module which initializes the scripts required
 * to handle the various functionality present on the favourites page, which is on
 * the default home page.
 */
class FavouritesLoader extends Loadable {
    load() {
    }
}