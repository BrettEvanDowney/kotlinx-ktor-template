/**
 * A data class which holds properties used within animation
 * sequences.
 */
class AnimationProperties {

    constructor(readonly animation: string, readonly animationTime: number, readonly animationDelay: number) {
    }
}